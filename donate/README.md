# Donate QR Codes

These images are used by the GitHub README donation section only:

- `vx.jpg` for WeChat
- `zfb.jpg` for Alipay

They stay outside `public/` so they are not copied into the app frontend build.
